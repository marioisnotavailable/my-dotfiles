// ==========================================
// MY NIRI CONFIGURATION (PORTED FROM HYPRLAND)
// ==========================================

// Tell apps not to draw their own titlebars/decorations
prefer-no-csd

// Make inactive windows slightly transparent
window-rule {
    match is-focused=false
    opacity 0.90
}

window-rule {
    match is-focused=true
    opacity 0.98
}

window-rule {
    match app-id=r#"^steam_app_"#
    open-fullscreen true
}


// Autostart Services
spawn-at-startup "xwayland-satellite"  // Bridge for older X11 apps that don't support Wayland natively
spawn-at-startup "swaybg" "-i" "/home/mirko/Pictures/wallpaperflare.com_wallpaper.jpg" "-m" "fill"
spawn-at-startup "env" "GSK_RENDERER=ngl" "ironbar"
spawn-at-startup "dunst"
spawn-at-startup "wl-paste" "--watch" "cliphist" "store"
spawn-at-startup "udiskie" "-t"
spawn-at-startup "nm-applet"
spawn-at-startup "polkit-gnome-authentication-agent-1"
spawn-at-startup "swayosd-server"

input {
    keyboard {
        xkb {
            layout "de"
        }
        repeat-delay 300
        repeat-rate 50
    }
    
    touchpad {
        click-method "button-areas"  // Right-click in bottom right, left-click everywhere else
        tap                          // Tap to left-click
        dwt                          // Disable while typing
        natural-scroll
    }
    
    mouse {
        accel-profile "flat"
    }

    focus-follows-mouse max-scroll-amount="0%"
}

layout {
    gaps 8
    center-focused-column "never"
    default-column-width { proportion 0.5; }
    
    focus-ring {
        width 3
        active-color "#89b4fa"
        inactive-color "#585b70"
    }
}

// Window rules
window-rule {
    match app-id="fuzzel"
    open-floating true
}

// Keybindings
binds {
    // --- Applications ---
    Mod+Q { spawn "wezterm" "start" "--always-new-process"; }
    Mod+B { spawn "firefox"; }
    Mod+E { spawn "dolphin" "--new-window"; }
    Mod+D { spawn "fuzzel"; }
    Mod+V { spawn "bash" "-c" "cliphist list | fuzzel --dmenu --prompt '󰅍 Clipboard: ' | cliphist decode | wl-copy && wtype -M ctrl -k v -m ctrl"; }
    
    Mod+Ctrl+O { spawn "wezterm" "-e" "ollama-chat"; }
    Mod+Ctrl+N { spawn "sunsetr-toggle"; }

    // --- Window Management ---
    Mod+C { close-window; }
    Mod+F { fullscreen-window; }
    Mod+M { maximize-column; }
    Mod+T { toggle-window-floating; }
    Mod+J { consume-or-expel-window-left; }

    // --- Focus Movement ---
    Mod+Left  { focus-column-left; }
    Mod+Right { focus-column-right; }
    Mod+Up    { focus-window-up; }
    Mod+Down  { focus-window-down; }

    // --- Window Moving (Swap) ---
    Mod+Alt+Left  { move-column-left; }
    Mod+Alt+Right { move-column-right; }
    Mod+Alt+Up    { move-window-up; }
    Mod+Alt+Down  { move-window-down; }
    
    // --- Resizing ---
    Mod+Shift+Left  { set-column-width "-10%"; }
    Mod+Shift+Right { set-column-width "+10%"; }
    Mod+Shift+Up    { set-window-height "-10%"; }
    Mod+Shift+Down  { set-window-height "+10%"; }

    // --- Workspaces (Focus) ---
    Mod+1 { focus-workspace 1; }
    Mod+2 { focus-workspace 2; }
    Mod+3 { focus-workspace 3; }
    Mod+4 { focus-workspace 4; }
    Mod+5 { focus-workspace 5; }
    Mod+6 { focus-workspace 6; }
    Mod+7 { focus-workspace 7; }
    Mod+8 { focus-workspace 8; }
    Mod+9 { focus-workspace 9; }
    
    // --- Workspaces (Move Window) ---
    Mod+Shift+1 { move-column-to-workspace 1; }
    Mod+Shift+2 { move-column-to-workspace 2; }
    Mod+Shift+3 { move-column-to-workspace 3; }
    Mod+Shift+4 { move-column-to-workspace 4; }
    Mod+Shift+5 { move-column-to-workspace 5; }
    Mod+Shift+6 { move-column-to-workspace 6; }
    Mod+Shift+7 { move-column-to-workspace 7; }
    Mod+Shift+8 { move-column-to-workspace 8; }
    Mod+Shift+9 { move-column-to-workspace 9; }

    // --- Screenshots ---
    Mod+Shift+S { spawn "sh" "-c" "grim -g \"$(slurp)\" - | wl-copy"; }
    Print { screenshot; }

    // --- Media & Volume (SwayOSD Integration) ---
    XF86AudioPlay { spawn "playerctl" "play-pause"; }
}
